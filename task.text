/*
Steven is still building his tip calculator, using the same rules as before: Tip 15% of the bill if the bill value is between 50 and 300, and if the value is different, the tip is 20%.
1. Write a function 'calcTip' that takes any bill value as an input and returns the corresponding tip, calculated based on the rules above (you can check out the code from first tip calculator challenge if you need to). Use the function type you like the most. Test the function using a bill value of 100.
2. And now let's use arrays! So create an array 'bills' containing the test data below.
3. Create an array 'tips' containing the tip value for each bill, calculated from the function you created before.
4. BONUS: Create an array 'total' containing the total values, so the bill + tip.
TEST DATA: 125, 555 and 44
HINT: Remember that an array needs a value in each position, and that value can actually be the returned value of a function! So you can just call a function as array values (so don't store the tip values in separate variables first, but right in the new array) 😉
GOOD LUCK 😀
*/                                 !                                                                           >
/*
Стівен все ще створює свій калькулятор чайових, використовуючи ті ж правила, що й раніше: дайте чайові 15% від суми рахунку, якщо вартість рахунку становить від 50 до 300, а якщо вартість інша, чайові становитимуть 20%.
1. Напишіть функцію 'calcTip', яка приймає будь-яке значення рахунку як вхідні дані та повертає відповідні чайові, розраховані на основі наведених вище правил (ви можете перевірити код із першого завдання калькулятора чайових, якщо потрібно). Використовуйте тип функції, який вам найбільше подобається. Перевірте функцію, використовуючи вартість рахунку 100.
2. А тепер скористаємося масивами! Отже, створіть масив «рахунки», що містить тестові дані нижче.
3. Створіть масив «чайові», що містить значення чайових для кожної банкноти, обчислене за допомогою функції, яку ви створили раніше.
4. БОНУС: створіть масив «total», що містить загальні значення, тож рахунок + чайові.
ДАНІ ТЕСТУ: 125, 555 і 44
ПІДКАЗКА: пам’ятайте, що для масиву потрібне значення в кожній позиції, і це значення насправді може бути повернутим значенням функції! Отже, ви можете просто викликати функцію як значення масиву (тому не зберігайте значення підказки спочатку в окремих змінних, а прямо в новому масиві) 😉
УСПІХІВ 😀
*/